# Optional for cloud sync. Vercel Blob creates this automatically when you
# connect a Blob store. Without it, deployed instances run local-only.
# For local `npm run dev`, this is not required; the local server stores
# encrypted vault files in .local/vaults.
BLOB_READ_WRITE_TOKEN=
